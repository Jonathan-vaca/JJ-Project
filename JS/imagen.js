window.addEventListener('DOMContentLoaded', () => {
    const topImageContainer = document.querySelector('.top-image-container');
    setTimeout(() => {
        topImageContainer.classList.add('show');
    }, 100);
});
